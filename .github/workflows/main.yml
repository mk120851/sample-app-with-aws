name: CI

on:
  push:
    branches:
      - master

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    
    steps:
    - uses: byrnedo/alpine-curl:latest
    - name: Use curl

    - name: run curl
      run: | 
        curl -X POST https://app.autify.com/api/v1/schedules/649 -H "Authorization:Bearer ${{ secrets.Autify_KEY_TOKEN }}"

